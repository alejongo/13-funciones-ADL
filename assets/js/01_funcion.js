// Transforma esta declaración de función a una función de expresión. El ejercicio debe
// quedar en un archivo nombrado
// `1_funcion.js`


// Ejercicio propuesto
function example(a, b, c) {
    return a + b + c
}

// Solución
const funcionExpresion = function example(a, b, c) {
    return a + b + c
}